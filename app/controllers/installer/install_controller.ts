// import type { HttpContext } from '@adonisjs/core/http'

export default class InstallController {
  handle() {
    // TODO: Single-action controller for setting up a Roost instance.
    // - Handles basic server settings
    // - Creates the owner user
    // - Sets the "APP_INSTALLED" & "APP_MAINTENANCE" global flag to false.
  }
}
